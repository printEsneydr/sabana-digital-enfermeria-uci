name: registro_uci
description: "Sábana Digital de Enfermería - Registro clínico de pacientes en la UCI del Hospital Universitario Departamental de Nariño (HUDN). Desarrollada por Esneyder Jesús Ibarra Rosero."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  freezed_annotation: ^2.4.4
  hooks_riverpod: ^2.5.2
  intl: ^0.20.2
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.4.0
  flutter_riverpod: ^2.4.0
  json_annotation: ^4.9.0
  fl_chart: ^0.69.0
  pdf: ^3.11.1
  printing: ^5.13.4
  path_provider: ^2.1.4
  signature: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.11
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  flutter_launcher_icons: ^0.14.3
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/logo.png
    - assets/images/logo_sabana_UCI.png
    - assets/images/background.jpg
    - fonts/Plus_Jakarta/PlusJakartaSans-Regular.ttf
    - fonts/Plus_Jakarta/PlusJakartaSans-Bold.ttf
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/logo.png"
  remove_alpha_ios: true
  web:
    generate: true
    image_path: "assets/images/logo.png"
  windows:
    generate: true
    image_path: "assets/images/logo.png"
    icon_size: 48
  macos:
    generate: true
    image_path: "assets/images/logo.png"

fonts:
    - family: Sacramento
      fonts:
        - asset: fonts/Sacramento/Sacramento-Regular.ttf
    - family: PlusJakartaSans
      fonts:
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-Regular.ttf
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-Italic.ttf
          style: italic
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-Bold.ttf
          weight: 700
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-BoldItalic.ttf
          weight: 700
          style: italic
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-ExtraBold.ttf
          weight: 800
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-ExtraBoldItalic.ttf
          weight: 800
          style: italic
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-ExtraLight.ttf
          weight: 200
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-ExtraLightItalic.ttf
          weight: 200
          style: italic
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-Light.ttf
          weight: 300
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-LightItalic.ttf
          weight: 300
          style: italic
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-Medium.ttf
          weight: 500
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-MediumItalic.ttf
          weight: 500
          style: italic
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-SemiBold.ttf
          weight: 600
        - asset: fonts/Plus_Jakarta/PlusJakartaSans-SemiBoldItalic.ttf
          weight: 600
          style: italic
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
